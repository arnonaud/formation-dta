package pizzeria.pizzeria_console;

import java.util.Scanner;

import abstractfactory.AbstractFactory;
import abstractfactory.FichierFactory;

import pizzeria.pizzeria_dao.PizzaDao;
import pizzeria.pizzeria_model.CategoriePizza;
import pizzeria.pizzeria_model.Pizza;



public class PizzeriaAdminConsoleApp {

	public static void main(String[] args) {
		Pizza p1 = new Pizza("PEP", "P�p�roni", 12.50, CategoriePizza.VIANDE);
      
        
        Pizza p2 = new Pizza("PEP", "P�p�roni", 12.50, CategoriePizza.VIANDE);
        
        System.out.println("p1=p2 ? " + p1.equals(p2));
		
		
		
		// AbstractFactory factory = new TableauFactory();
		AbstractFactory factory = new FichierFactory();

		PizzaDao pizzaDao = factory.getService().getPizzas();
		Scanner sc = new Scanner(System.in);
		IhmUtil ihmUtil = new IhmUtil(sc, pizzaDao);

		Menu menu = new Menu(ihmUtil);
		menu.demarrer();

	}

}
